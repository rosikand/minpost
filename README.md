# `minpost`

A minimal Python package for post-training language models.

## Installation

```bash
pip install -e .
```

This project uses `pyproject.toml` (PEP 621) with `setuptools` as the build backend.

## Usage

```python
from minpost import Trainer

# TODO: Add usage example
```

## Structure

```
minpost/
├── minpost/                # Main package
│   ├── __init__.py
│   ├── trainer.py          # Core training logic
│   ├── data.py             # Data loading and processing
│   └── utils.py            # Utility functions
├── tests/                  # Test suite
├── examples/               # Example scripts
├── pyproject.toml          # Package configuration
└── README.md
```
